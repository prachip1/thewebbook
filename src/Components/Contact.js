import React from "react";
import transition from "../transition";

const Contact = () =>{
    return(
        <div>
            <h1>Contact Us</h1>
        </div>
    )
}

export default transition(Contact);